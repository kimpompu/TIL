# 제곱수 판별하기

```Java
class Solution {
    public int solution(int n) {
        return (Math.sqrt(n) % 1 == 0) ? 1 : 2;
    }
}
```

## Math.sqrt(double d)
- 제곱근 계산
- `double` 반환

## Math.pow(double d, double n)
- n번 거듭 제곱한 값 계산
- `double` 반환

## 정수인지 확인하기
- `double % 1 == 0` 은 정수
# 종이 자르기

```Java
class Solution {
    public int solution(int M, int N) {
        int answer = (M*N)-1;
        return answer;
    }
}
```

## 총 횟수 
(가로 가위질) + (세로 가위질)
$=(M - 1) + M \times (N - 1)$
$= M - 1 + MN - M$
$= MN - 1$
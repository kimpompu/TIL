# 자릿수 더하기

```Java
class Solution {
    public int solution(int n) {
        
        String str = String.valueOf(n);
        int answer = 0;
        
        for(char c : str.toCharArray()){
            answer += (c-'0');
        }
        
        
        
        return answer;
    }
}
```
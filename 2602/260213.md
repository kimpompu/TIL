# 260213 TIL

## 삼각형 조건 로직
1. 주어진 두 변 중 큰 값을 $max$, 작은 값을 $min$이라고 정의
2. $max$가 가장 긴 변일 때, 새로운 변 $x$는 $max - min < x \le max$ 
2-2. $x$가 가장 긴 변일 때, 새로운 변 $x$는 $max < x < max + min$
3. $x$는 `(두 변의 차)` < $x$ < `(두 변의 합)` 을 만족해야함
4. 이 범위에 속하는 정수의 개수는 (작은 변의 길이 $\times 2 - 1$)로 계산


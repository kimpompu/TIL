# 260223 TIL

## 2차원 배열 좌표
- 2차원 배열 **왼쪽 상단**이 $(0,0)$
    ```
    [0,0] [0,1] [0,2]
    [1,0] [1,1] [1,2] 
    [2,0] [2,1] [2,2]
    ```

## 사용 메소드
| 메소드 | 설명 | 사용 예시 |
| :--- | :--- | :--- |
| `Arrays.fill(array, value)` | 배열의 모든 요소를 지정된 값으로 입력 | `Arrays.fill(minClick, MAX)` |
| `Integer.MAX_VALUE` | `int` 타입이 가질 수 있는 가장 큰 정수 값 반환 | |

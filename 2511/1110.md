# 251110 TIL

## DTO가 데이터 변환을 책임져야 하는 이유

- `fromEntity`, `toEntity` 를 통한 데이터 변환의 책임 주체는 DTO이다.

## **1. 단일 책임 원칙(SRP) 준수**

- DTO는 계층간 데이터 이동을 담당하기 때문
- 데이터 변환의 책임은 DTO에게 있다.

## **2. 무분별한 getter 사용 지양**

- `object.getXX()` 사용을 지양한다.
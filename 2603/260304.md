# 260304 TIL

## 1차원 배열의 인덱스 $k$를 $n$행 $n$열의 2차원 좌표로 변환
- 행(row): $k / n$
- 열(col): $k \% n$

## 행렬의 곱셈
## 곱셈한 행렬의 크기
- 결과 행렬의 크기는 **arr1의 행 개수 $\times$ arr2의 열 개수**

### 행렬 곱셈의 조건
- `앞 행렬의 열 개수`와 `뒤 행렬의 행 개수`가 반드시 `일치`